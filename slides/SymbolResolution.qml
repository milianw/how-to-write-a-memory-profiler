import SlideViewer 1.0

SlideSet {
    title: "Symbol Resolution"
    Slide {
        text: "* elf mappings
               * /proc/self/mappings + addr2line
               * dl_iterate_phdr: main exe is special
               * addr2line
               * basic elfutils setup
               * demangling"
    }
}
